[pytest]

addopts =
    --strict
    --pdbcls=tests:Debugger

    -r sxX

    --cov={{cookiecutter.plugin_name}}
    --cov-report=html
    --cov-report=term-missing:skip-covered
    --no-cov-on-fail

cache_dir = .cache

markers =
